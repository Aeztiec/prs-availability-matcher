# Note: timings/*.html are NOT ignored - they are the source of truth for
# which competitions exist, and the Action rebuilds docs/data.js from them.

__pycache__/
*.pyc

# Bot secrets - the Discord token must never be committed to a public repo.
.env
*.db
*.sqlite3

# The player sheet has Roblox user IDs and wages - never commit it to a public repo.
data/players.csv
